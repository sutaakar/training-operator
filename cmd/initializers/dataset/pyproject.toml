[project]
name = "dataset-initializer"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "huggingface-hub>=1.29.0,<1.30",
  "opendal>=0.47.6",
  "kubernetes>=36.0.3",
]

[tool.uv]
package = false
