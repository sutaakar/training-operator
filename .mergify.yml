pull_request_rules:
  - name: Auto-approve lake-gate PRs
    conditions:
      - base=stable
      - label=lake-gate
      - -draft
      - -closed
    actions:
      review:
        type: APPROVE

  - name: Auto-merge lake-gate PRs to stable
    conditions:
      - base=stable
      - label=lake-gate
      - -draft
      - -closed
      - "#approved-reviews-by>=1"
    actions:
      merge:
        method: fast-forward
